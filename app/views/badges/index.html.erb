<div class="container">
  <div class="mt-3">
    <% if @badges.blank? %>
      <h3> You dont have any badges yet </h3>
    <% else %>
      <%= render @badges %>
    <% end %>
  </div>
  <div class="mt-3">
    <%= link_to 'Available badges', show_all_path %>
  </div>
</div>
